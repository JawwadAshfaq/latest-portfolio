import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['jawwadashfaq.vercel.app'], // If you need to load images from external domains
  },
};

export default nextConfig;
